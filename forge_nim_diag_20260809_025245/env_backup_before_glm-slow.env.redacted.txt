# FORGE local secrets
# This file is gitignored. Do not commit real keys.

# Search API fallback keys (optional but recommended for resilient web search)
TAVILY_API_KEY="<redacted:58>"
SERPER_API_KEY="48bba30f4344cc89830967c0aad436a0fc3e0cea"
BRAVE_API_KEY=""

# Optional API proxy. Empty string disables explicit proxy for API fallbacks.
# Default in code: http://127.0.0.1:7890
NETWORK_SEARCH_API_PROXY="http://127.0.0.1:7890"

# Privacy Gateway local DB encryption key. Use a long local random string.
PII_MAP_ENCRYPTION_KEY="replace-with-local-random-32chars"

# 商业模型(走英伟达)
DEEPSEEK_API_KEY="nvapi-<redacted>"
GLM_API_KEY="nvapi-<redacted>"

# 工具调用配置
FORGE_ALLOW_UNKNOWN_MODEL_FALLBACK=0

NIM_PROXY_READ_TIMEOUT_SECONDS=360
NIM_PROXY_MAX_ATTEMPTS_PER_REQUEST=1

FORGE_LOCAL_FORWARD_TOOLS=1
FORGE_REMOTE_FORWARD_TOOLS=1

FORGE_LOCAL_DEFAULT_MAX_TOKENS=1024
FORGE_LOCAL_MAX_TOKENS=2048
FORGE_REMOTE_MAX_TOKENS=16384

FORGE_SORT_TOOLS_FOR_CACHE=1
FORGE_COUNT_TOKENS_DIVISOR=4

FORGE_TOOL_SELECTION_ENABLED=1
FORGE_TOOL_SELECTION_THRESHOLD=8
FORGE_TOOL_SELECTION_DESC_MAX=100
FORGE_TOOL_SELECTION_CACHE_SIZE=1000
FORGE_TOOL_SELECTION_TIMEOUT_S=20
FORGE_TOOL_SELECTION_MAX_TOKENS=150
FORGE_TOOL_SELECTION_INTENT_MAX_CHARS=2000
FORGE_TOOL_SELECTOR_POLICY_VERSION=v2
FORGE_CORE_TOOLS=Read,Bash,Edit,Grep,Glob,LS

FORGE_SERIALIZE_LOCAL_PORTS=1
FORGE_LOCAL_RETRY_COUNT=0
FORGE_REMOTE_RETRY_COUNT=2

FORGE_STREAM_PING_INTERVAL_SECONDS=10


# ── 上下文预算 guard（防 429 膨胀 + 防 400 超长）──────────────
# SOFT：会话超此 token 数就压缩历史，防 NIM 大请求 429 + 降 TTFB。
# 旧 soft=162201 太高从未触发；新 32000(≈128KB body) 让 guard 真正生效。
FORGE_CTX_SOFT_TOKENS=12000
# 远程路径工具选择：NIM 全量转发 25 工具致 289KB/请求，开启后用本地模型选工具，body 降至 ~39KB。
FORGE_REMOTE_TOOL_SELECTION=1
FORGE_REMOTE_SELECTOR_PORT=8080

# ── NIM 轻量代理配置──────────────
# NIM keys
NVIDIA_API_KEY_1="nvapi-<redacted>"
NVIDIA_API_KEY_2="nvapi-<redacted>"

# 开启 sidecar
FORGE_USE_NIM_PROXY=1
NIM_PROXY_HOST="127.0.0.1"
NIM_PROXY_PORT=4010
NIM_PROXY_BASE_URL="http://127.0.0.1:4010/v1"
NIM_PROXY_API_KEY="nim-proxy-local"

# 限速
NIM_PROXY_PER_KEY_RPM=35
NIM_PROXY_PER_KEY_CONCURRENCY=1
NIM_PROXY_DEFAULT_COOLDOWN_SECONDS=300
NIM_PROXY_RETRY_AFTER_CAP_SECONDS=900
NIM_PROXY_QUEUE_TIMEOUT_SECONDS=900

# 模型
NIM_PRIMARY_MODEL="z-ai/glm-5.2"
NIM_PROXY_ENABLE_FALLBACK=0
NIM_FALLBACK_MODEL="deepseek-ai/DeepSeek-V4-Pro"

# Smart Proxy 上下文与工具选择

FORGE_TOOL_SELECTION_MAX=8
FORGE_TOOL_SCHEMA_BYTE_BUDGET=32768
# RPM 上限：NIM 免费档无 ratelimit 头，只能本地预防。35 RPM 当日触发 324×429，
# 降到 15 为安全保守值。待社区 RPM 实测数据确认后精调。
FORGE_RPM_MAX=35
# 远程并发上限：NIM 免费档实测并发 ~5（20 并发→15×429/5×200），从源头卡并发。
FORGE_REMOTE_MAX_CONCURRENCY=1
# 429 风暴熔断器：连续 2 次 429 即熔断，冷却 45s 内新请求本地等待不裸打上游。
FORGE_CIRCUIT_BREAKER_TRIP=2
FORGE_CIRCUIT_BREAKER_COOLDOWN=45.0
# 流式路径 429/读异常退避重试次数（未发内容时）。
FORGE_STREAM_REMOTE_RETRY_COUNT=2
NIM_PROXY_REQUEST_WALL_TIMEOUT_SECONDS=600
NIM_PROXY_SESSION_AFFINITY=0
FORGE_CTX_KEEP_RECENT_TURNS=4
FORGE_CTX_TRUNC_TOOL_RESULT_CHARS=800
