<!--
创建/修改该文件的LLM大模型：Claude Sonnet 4.5 (via Arena.ai Agent Mode)
创建时间（北京时间，精确到秒）：2026-06-10 23:03:36 CST
-->

# HANDOFF —— 接力交接文档（意外中止时，接续 Agent 必读）

> 这份文档的唯一目的：**让任何一个全新的 Agent，在不依赖原始对话的情况下，能在 5 分钟内接手本项目并继续开发。**
> 如果你是接手的 Agent：**先读完本文件，再读 `docs/PROJECT_STATE.md` 和 `docs/DEV_LOG.md`，然后才能动手。**

---

## 0.0.1 ⭐ 工作规则（老板第16轮新增，必须遵守）

- **R1 · 模型选型不限于本地已有**：涉及开源模型时，若某任务有**更合适的开源模型**，要主动建议老板
  （前提：**本机硬件能满足**，M1 Max 64GB）。老板可通过 ollama 拉取。不要只局限于当前已装模型。
- **R2 · 决断前先调研主流方案**：任何需要老板做决断的点，**先调研当前世界上最新最主流的备选方案**
  （用 web 搜索），把候选方案 + 优劣摆给老板，老板在此基础上再形成想法。不要让老板凭空决断。
- **R3 · 操作指示必须保姆级详细**（老板第18轮）：给老板的每条操作指示都要写清——
  ① 用第几个终端（终端A/B，是否需另开）；② `cd` 到哪个**绝对路径**目录；③ 虚拟环境**是否需要激活、激活哪个**（`source xxx/bin/activate`，提示符应变成什么）；
  ④ 每条命令的**预期输出**；⑤ 常见报错及解决。不要假设老板记得上下文。

## 0.0 ⭐ 项目定位（第7轮老板明确，最高优先级，别跑偏）

- **我们真正的产品 = 这套"AI 项目孵化工厂"本身**。目标是把这条流水线打磨好、把它的**能力边界**摸清楚，
  以便老板**以后能反复用它在本地开发各种项目**。
- 当前选的"讨债/催收"试点**只是陪练沙包**——用来压测工厂、暴露问题，**不是要真去把讨债系统开发出来**。
- **角色定位：老板=教练，Agent=陪练。** 重心永远是"工厂好不好用、边界在哪"，不是某个具体业务。
- 因此：遇到试点项目的具体业务决策，要意识到它的价值在于"检验工厂的某个能力"，
  不要一头扎进去当成正式交付项目来做。试点过程中暴露的工厂缺陷、踩的坑、验证的能力，才是真正要沉淀的成果。

## 0. 一句话说清这个项目

我们在为一位**独立开发者**（产品负责人，下称"老板"）建造一套叫 **FORGE Factory（项目孵化工厂）** 的工作体系：
能在他的 **macOS M1 Max 64GB** 单机上，把"模糊想法"反复、可重复地变成"能跑的 AI 软件项目"。

它**不是**一个传统的单体软件，而是一套**脚手架 + 配置 + 脚本 + 知识库 + 工作流规范**，驱动 VS Code 里的 **Claude Code** 按五阶段状态机运行。

权威来源（只信这两份，其他都是衍生）：
- `docs/AI 项目孵化工厂需求说明书-V2.docx`（要什么）
- `docs/AI 项目孵化工厂架构设计书-V4.docx`（怎么做）

---

## 1. 角色与协作规则（必须遵守）

- **老板是产品负责人，他做决定，你负责实现。**
- 关键决策点（架构、选型、风险接受度、最终验收）**必须停下来问他**，用 `ask_user`，给选项，别替他赌。
- 别用技术黑话轰炸他，把东西翻译成人话。
- 如果他想得太复杂或走错方向，**直接说**。坦诚技术限制。

---

## 2. 当前运行环境的真相（非常重要，别踩坑）

| 维度 | 老板的真机（目标环境） | 我们（Agent）的沙箱 |
|---|---|---|
| 系统 | macOS Sequoia 15.6.1 | Linux 云沙箱 |
| 芯片 | Apple M1 Max 64GB | x86 容器 |
| Python | 3.11 | 3.13（沙箱） |
| 包管理 | **uv**（强制） | 沙箱无 uv，用 pip 临时验证 |
| 本地模型 | Ollama：qwen3.6:35b-a3b-q8_0 / qwen3:14b / bge-m3 / qwen3-embedding:8b | **沙箱没有 Ollama** |
| 云端模型 | GLM via 代理转发（**Key 待老板填**） | **沙箱无 GLM Key** |
| 执行底座 | Claude Code in VS Code | **沙箱无 Claude Code** |

**对齐策略（老板已拍板"两者都要"）：**
- 能在沙箱跑通的纯逻辑（CLI、状态机、TASK_GRAPH 解析、Gate 流转、文档校验）→ 我就地写测试并真实跑通。
- 依赖真实模型 / Claude Code 的链路 → 给出**真机验证清单**（见 `docs/REAL_MACHINE_VALIDATION.md`），由老板在 Mac 上验证。

---

## 3. 老板的真机测试路径与补丁约定（每轮必做）

- 老板的真机项目路径：`/Users/naturist/MusicProject/AI-Project-Incubation-Factory`
- **每次改动文件后，必须在 workspace 打一个 zip 补丁包**，放在 `test02/patches/` 下，命名格式：
  `patch_YYYYMMDD_HHMMSS_<本轮主题>.zip`（北京时间）
- 老板用 `unzip -o <补丁路径>` 覆盖本地旧文件。
- **补丁包内的目录结构必须与项目根（`test02/` 内容）对齐**，这样 `unzip -o` 到他的项目根即可覆盖。
- `patches/` 已在 `.gitignore` 中忽略，不入库。
- **⚠️ 第4轮起新规则（老板要求）**：补丁包**只打包本轮改动的文件/目录**，不要再全量打包。
  打包前先想清楚这一轮动了哪些文件，只 zip 这些。

## 3.1 双向同步：通过 GitHub 拉取真机产物（第4轮起）

- 老板会把真机的**测试产物 / 日志 / 错误输出**写成文件，从本地 `git push` 到
  `https://github.com/naturefoodhuman/test02.git`。
- 接续 Agent / 我可以直接 `git clone`/`git pull` 该仓库来读取这些产物分析，**不再需要老板手动复制粘贴**。
- 因此：当需要老板提供运行结果时，优先建议他"把结果存成文件 push 上来"，我去拉。
- 我也可以**主动在 workspace 生成需要老板运行的测试脚本/诊断脚本**，让他跑完把输出 push 回来。

---

## 4. 关键约束（架构书 C1–C7，决策时的红线）

- **C1**：境外闭源模型（ChatGPT/Claude 网页）= 人工手动交互，仅限关键节点，**无 API**。
- **C2**：可自动调用的最强云端 = **GLM**（via 代理转发），不可用时自动切本地，不中断。
- **C3**：本地主力 = `qwen3.6:35b-a3b-q8_0`。
- **C4**：本地 fallback = `qwen3:14b`。
- **C5**：无 OpenRouter，无其他外部云端路由。
- **C6**：执行底座 = Claude Code in VS Code（唯一底座）。
- **C7**：无月度预算硬限制（但成本要可观测）。

---

## 5. 文件头规范（每个新建/修改的代码或文档都必须遵守）

每个文件头部注释必须注明：
1. 创建/修改该文件的 **LLM 大模型名称**
2. **时间**（北京时间，精确到秒，CST）

示例（Markdown 用 HTML 注释，Python/Shell/YAML 用 `#`）：
```
# 创建/修改该文件的LLM大模型：Claude Sonnet 4.5 (via Arena.ai Agent Mode)
# 创建时间（北京时间，精确到秒）：2026-06-10 23:03:36 CST
```

---

## 6. 接手流程（接续 Agent 照着做）

1. 读本文件（HANDOFF.md）。
2. 读 `docs/PROJECT_STATE.md` —— 看当前进度到哪、下一步是什么。
3. 读 `docs/DEV_LOG.md` —— 看每轮做了什么、为什么这么做。
4. 读 `docs/DECISIONS.md` —— 看所有已拍板的决策（别推翻已定的事）。
5. 如果要继续开发：从 `PROJECT_STATE.md` 的"下一步"接着干。
6. 干完后：**更新这 4 份维护文档 + 打补丁包**，缺一不可。

---

## 7. 维护文档清单（每轮必须同步更新）

| 文档 | 作用 |
|---|---|
| `HANDOFF.md`（本文件） | 接力总入口，环境真相、规则、红线 |
| `docs/PROJECT_STATE.md` | 当前进度快照 + 下一步 + 待办 |
| `docs/DEV_LOG.md` | 逐轮开发日志（做了啥、为啥） |
| `docs/DECISIONS.md` | 决策记录（老板拍板的事） |
| `docs/REAL_MACHINE_VALIDATION.md` | 真机验证清单（沙箱跑不了的链路） |
| `docs/CHANGELOG.md` | 需求"新增/删除/改动"的变动说明 |

---

## 8. 变更记录（本文件自身）

| 时间(CST) | 变更 | 模型 |
|---|---|---|
| 2026-06-10 23:03:36 | 初版创建：接力文档体系奠基 | Claude Sonnet 4.5 |
