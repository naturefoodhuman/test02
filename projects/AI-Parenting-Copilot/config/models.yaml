# 创建/修改该文件的LLM大模型：Arena.ai Agent Mode - Execution Lead Engineer
# 创建时间（北京时间）：2026-07-08 23:55:00

# Project-level model aliases. Actual runtime and model process ownership stays in
# factory root config/models.yaml and config/model_runtime.yaml.
models:
  parenting-local-default:
    provider: smart_proxy
    description: Local-first default model routed through factory Smart Proxy.
    factory_model_alias: claude-sonnet-local
  parenting-cloud-redacted:
    provider: smart_proxy
    description: Optional redacted cloud fallback routed through factory Smart Proxy.
    requires_privacy_gateway: true
