# 创建/修改该文件的LLM大模型：Arena.ai Agent Mode
# 创建时间（北京时间）：2026-07-09 05:55:00

channels:
  fcm: {enabled: true, priority: 1}
  mac_speaker: {enabled: true, priority: 2}
  app_fullscreen: {enabled: true, priority: 3}
  camera_speaker: {enabled: false, priority: 4}
escalation:
  red_repeat_seconds: 60
  red_escalate_seconds: 90
