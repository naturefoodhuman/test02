# 创建/修改该文件的LLM大模型：Arena.ai Agent Mode - Execution Lead Engineer
# 创建时间（北京时间）：2026-07-08 23:35:00


config:
  edition: 3

streams:
  global:
    auto_subscribe: true
    queries:
      # APC-T012 will replace this with family-scoped sync streams.
      # The table itself is created by APC-T004.
      - SELECT * FROM observation_event WHERE is_deleted = false
