# 创建/修改该文件的LLM大模型：Arena.ai Agent Mode
# 创建时间（北京时间）：2026-07-08 23:35:00


replication:
  connections:
    - type: postgresql
      uri: postgresql://parenting:parenting@postgres:5432/parenting
      sslmode: disable

storage:
  type: postgresql
  uri: postgresql://parenting:parenting@postgres:5432/powersync_storage
  sslmode: disable

port: 8080

sync_config:
  path: /config/sync-config.yaml

client_auth:
  supabase: false

telemetry:
  disable_telemetry_sharing: true

system:
  logging:
    level: info
    format: text
