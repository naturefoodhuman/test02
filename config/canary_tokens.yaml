# 创建/修改该文件的LLM大模型：Arena.ai Agent Mode
# 创建时间（北京时间）：2026-06-22 20:58:00

# Canary tokens for Privacy Gateway L7.
# Tokens are fake secrets deliberately injected into private/test data.
# If any token appears in model output, transcript, browser logs, or markdown,
# the pipeline must block immediately.
canary_tokens:
  - "AI_CANARY_DO_NOT_LEAK_2026"

# Optional explicit regex patterns. Keep empty unless a broader pattern is needed.
canary_patterns: []
