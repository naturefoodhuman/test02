# 创建/修改该文件的LLM大模型：Arena.ai Agent Mode - Execution Lead Engineer
# 创建时间（北京时间）：2026-07-01 00:00:00

gateways:
  clipboard:
    enabled: true
    requires_human_copy: true
    requires_human_import: true
    audit_level: "full"

  api:
    enabled: false
    reason: "future gateway; disabled in current Clipboard-first main flow"

  mcp:
    enabled: false
    reason: "future gateway; disabled until explicit architecture approval"

  browser:
    enabled: false
    reason: "future gateway; disabled until explicit architecture approval"

  cloud_agent:
    enabled: false
    reason: "future gateway; disabled until explicit architecture approval"
